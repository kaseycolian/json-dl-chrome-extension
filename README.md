# json-dl-chrome-extension
Chrome Extension to Download JSON from Requests Responses


To install:

Go to chrome://extensions
Enable Developer Mode (top right)
Click Load unpacked → select `/src`
Open the extension popup to configure, then open DevTools on any page